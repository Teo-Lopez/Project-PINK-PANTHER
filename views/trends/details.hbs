<h1>{{titleMain}} 🚀</h1>
<br>
<div class="container">
    <div class="row ">
        <div class="col">
            <a href="/fotograma/crear" class="btn btn-dark">Creacion</a>
        </div>
        <div class="col">
            <a href="/tendencias" class="btn btn-dark">Tendencias</a>
        </div>
    </div>
</div>

<div class="container">
    <div class="row">
        <div class="col-4 column">
            <h4> <span> Tag:</span>{{tagUploads.[0].tagId.name}}</h4>
            {{#each tagUploads}}
            <img class="resolve" src="{{img}}" alt="imagen">
            {{#if ../isUser}}
            <a href='/fotograma/detalles/{{id}}' class="btn btn-dark"> ir a narnia</a>
            {{/if}}
            {{/each}}
        </div>
    </div>
</div>